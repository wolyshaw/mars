import React, { Component } from 'react'
import { Layout } from 'antd'

const { Footer } = Layout

const Foot = props => {
  return (
    <Footer style={{ textAlign: 'center' }}>
      Foot
    </Footer>
  )
}

export default Foot
