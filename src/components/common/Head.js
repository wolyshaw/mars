import React, { Component } from 'react'
import { Layout } from 'antd'

const { Header } = Layout

const Head = props => {
  return (
    <Header style={{ background: '#fff', padding: 0 }} />
  )
}

export default Head
